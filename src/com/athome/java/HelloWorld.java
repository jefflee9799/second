package com.athome.java;

import java.util.ArrayList;
import java.util.Arrays;

import static java.lang.System.*;

public class HelloWorld {
    //psvm
//    sout
//    public static final num;
//    private static final num1;


    public static void main(String[] args) {
        ArrayList<Object> objects = new ArrayList<>();

        objects.add(1);
        objects.add(2);
        objects.add(3);
        if (objects != null) {
//

        }
        for (int i = 0; i < objects.size(); i++) {
            for (Object integer : objects)
                if (integer == null) {
                    if (integer != null) {

                    }
                }
        }
        out.println("sdds");
        out.println("args = " + Arrays.deepToString(args));
        out.println("HelloWorld.main");
        out.println("args = " + args);
        for (String arg : args) {
            out.println("dsad");
            for (int j = 0; j < args.length; j++) {
                String s = args[j];
                out.println();

            }
        }
        }
    }


}
